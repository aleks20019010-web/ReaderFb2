<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="250dp"
    android:orientation="vertical"
    android:padding="6dp"
    android:clickable="true"
    android:focusable="true">

    <com.google.android.material.card.MaterialCardView
        style="@style/CardViewStyle"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:strokeWidth="0dp"
        app:cardBackgroundColor="@android:color/transparent"
        app:cardUseCompatPadding="false">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_card_glass"
            android:foreground="?android:attr/selectableItemBackground">

            <!-- Cover frame (top-aligned, takes fixed proportional height) -->
            <FrameLayout
                android:id="@+id/coverContainer"
                android:layout_width="0dp"
                android:layout_height="145dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent">

                <!-- Gradient background (fallback) or loaded cover -->
                <View
                    android:id="@+id/vCoverBackground"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="#e94560" />

                <ImageView
                    android:id="@+id/ivCover"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    android:contentDescription="Обложка книги" />

                <!-- Semi-transparent gloss/glow overlay for realistic dust-jacket shine -->
                <View
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@drawable/bg_cover_glow" />

                <!-- Delete overlay button -->
                <ImageButton
                    android:id="@+id/btnDelete"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_gravity="top|end"
                    android:layout_margin="4dp"
                    android:background="@android:drawable/presence_offline"
                    android:backgroundTint="#80000000"
                    android:src="@android:drawable/ic_menu_close_clear_cancel"
                    android:contentDescription="Удалить"
                    android:tint="@android:color/white"
                    android:padding="4dp" />
            </FrameLayout>

            <!-- Book Title: max 2 lines, truncated, aligned below cover -->
            <TextView
                android:id="@+id/tvBookTitle"
                android:layout_width="0dp"
                android:layout_height="38dp"
                android:layout_marginTop="6dp"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:ellipsize="end"
                android:maxLines="2"
                android:textColor="@color/text_title"
                android:textSize="13sp"
                android:textStyle="bold"
                android:gravity="top|start"
                app:layout_constraintTop_toBottomOf="@id/coverContainer"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- Book Author: max 1 line, truncated, aligned below title -->
            <TextView
                android:id="@+id/tvBookAuthor"
                android:layout_width="0dp"
                android:layout_height="18dp"
                android:layout_marginTop="2dp"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/text_author"
                android:textSize="11sp"
                android:gravity="center_vertical|start"
                app:layout_constraintTop_toBottomOf="@id/tvBookTitle"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- Book Series: max 1 line, truncated, aligned below author -->
            <TextView
                android:id="@+id/tvBookSeries"
                android:layout_width="0dp"
                android:layout_height="14dp"
                android:layout_marginTop="1dp"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/text_secondary"
                android:textSize="9sp"
                android:gravity="center_vertical|start"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@id/tvBookAuthor"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- Single progress bar pinned perfectly to the bottom of the card -->
            <ProgressBar
                android:id="@+id/pbReadingProgress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="0dp"
                android:layout_height="4dp"
                android:layout_marginBottom="4dp"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:progress="0"
                android:max="100"
                android:progressTint="@color/accent"
                android:progressBackgroundTint="#20FFFFFF"
                android:visibility="gone"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.card.MaterialCardView>
</LinearLayout>
