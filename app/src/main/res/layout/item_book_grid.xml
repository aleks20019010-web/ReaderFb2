<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    app:cardCornerRadius="14dp"
    app:cardElevation="3dp"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="#231735"
    app:cardPreventCornerOverlap="false"
    app:cardUseCompatPadding="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Cover container taking most of the card height -->
        <FrameLayout
            android:id="@+id/coverContainer"
            android:layout_width="match_parent"
            android:layout_height="195dp"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:paddingHorizontal="14dp"
            android:paddingTop="12dp"
            android:paddingBottom="10dp">

            <!-- Deep Glow: blurred background glow that automatically matches cover's color -->
            <View
                android:id="@+id/vCoverGlow"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_margin="3dp"
                android:background="@android:color/transparent" />

            <!-- Realistic 3D volume book card -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/cvBookCover"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:cardCornerRadius="10dp"
                app:cardElevation="3dp"
                app:strokeWidth="1dp"
                app:strokeColor="#25FFFFFF"
                app:cardBackgroundColor="#1A1A2E"
                app:cardPreventCornerOverlap="false"
                app:cardUseCompatPadding="false">

                <FrameLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent">

                    <!-- Fallback background gradient -->
                    <View
                        android:id="@+id/vCoverBackground"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:background="#1A1A2E" />

                    <!-- Cover Image -->
                    <ImageView
                        android:id="@+id/ivCover"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:scaleType="centerCrop"
                        android:contentDescription="Обложка книги" />

                    <!-- Cover letter fallback -->
                    <TextView
                        android:id="@+id/tvCoverLetter"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center"
                        android:textColor="#FFFFFF"
                        android:textSize="28sp"
                        android:textStyle="bold"
                        android:visibility="gone" />

                    <!-- Book spine crease 3D overlay -->
                    <View
                        android:layout_width="16dp"
                        android:layout_height="match_parent"
                        android:layout_gravity="start"
                        android:background="@drawable/bg_book_spine" />

                    <!-- Book page-edge highlight on right -->
                    <View
                        android:layout_width="2.5dp"
                        android:layout_height="match_parent"
                        android:layout_gravity="end"
                        android:background="#20FFFFFF" />

                    <!-- Progress bar at the very bottom of the cover -->
                    <ProgressBar
                        android:id="@+id/pbReadingProgress"
                        style="?android:attr/progressBarStyleHorizontal"
                        android:layout_width="match_parent"
                        android:layout_height="4dp"
                        android:layout_gravity="bottom"
                        android:progress="0"
                        android:max="100"
                        android:progressTint="@color/accent"
                        android:progressBackgroundTint="#20FFFFFF"
                        android:visibility="gone" />
                </FrameLayout>
            </com.google.android.material.card.MaterialCardView>
        </FrameLayout>

        <!-- Text container under the cover, inside the CardView -->
        <LinearLayout
            android:id="@+id/textContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="10dp"
            android:paddingTop="8dp"
            android:paddingBottom="10dp">

            <TextView
                android:id="@+id/tvBookTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="2"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:textStyle="bold"
                android:lineSpacingMultiplier="1.1" />

            <TextView
                android:id="@+id/tvBookAuthor"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="3dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="#B0A4C4"
                android:textSize="10.5sp" />

            <TextView
                android:id="@+id/tvBookSeries"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="#887C9E"
                android:textSize="9.5sp"
                android:visibility="gone" />

            <!-- Hidden components for compatibility with existing adapter logic -->
            <ImageButton
                android:id="@+id/btnDelete"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:visibility="gone" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
