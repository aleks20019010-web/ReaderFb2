<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- The main content view -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Dynamic Starry Parallax Background -->
        <include
            android:id="@+id/starry_bg"
            layout="@layout/layout_galaxy_bg" />

        <FrameLayout
            android:background="@android:color/transparent"
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />


    </FrameLayout>

    <!-- The navigation drawer layout -->
    <FrameLayout
        android:id="@+id/drawer_layout_container"
        android:layout_width="320dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@drawable/bg_drawer"
        android:elevation="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <com.google.android.material.navigation.NavigationView
                android:id="@+id/nav_view"
                style="@style/Widget.NightRead.NavigationView"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:background="@android:color/transparent"
                app:backgroundTint="@android:color/transparent"
                app:menu="@menu/menu_drawer" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/divider"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp" />

            <!-- Custom button at the bottom -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_searchfloor"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:layout_marginTop="12dp"
                android:layout_marginBottom="16dp"
                android:text="SearchFloor"
                android:textColor="@color/btn_text"
                app:backgroundTint="@color/btn_bg"
                app:cornerRadius="12dp"
                app:icon="@drawable/ic_action_search"
                app:iconTint="@color/btn_text"
                app:iconGravity="textStart"
                android:textAllCaps="false" />

        </LinearLayout>
    </FrameLayout>

</androidx.drawerlayout.widget.DrawerLayout>
